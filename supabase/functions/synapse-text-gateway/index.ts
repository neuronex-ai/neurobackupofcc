const corsHeaders={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization,x-client-info,apikey,content-type"};const reply=(data,status=200)=>new Response(JSON.stringify(data),{status,headers:{...corsHeaders,"Content-Type":"application/json"}});const routes={"/dashboard":["navigate","dashboard"],"/agenda":["open_daily_schedule",null],"/pacientes":["navigate","patients"],"/financeiro":["navigate","finance"],"/notas":["navigate","notes"],"/teleconsulta":["navigate","teleconsultation"]};function safeAction(value){if(!value||typeof value!=="object")return null;const data=value.data||value.payload||value;if(value.type==="interface_action")return value;if(value.type!=="navigation_action"||typeof data.path!=="string")return value;const mapped=routes[data.path.replace(/\/$/,"")];if(!mapped)return null;return{type:"interface_action",data:{action:mapped[0],target:mapped[1]||undefined,reason:data.reason}}}Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response(null,{headers:corsHeaders});try{const auth=req.headers.get("Authorization")||"";const body=await req.json();const url=Deno.env.get("SUPABASE_URL");const anon=Deno.env.get("SUPABASE_ANON_KEY");const primary=await fetch(`${url}/functions/v1/gemini-text-chat`,{method:"POST",headers:{Authorization:auth,apikey:anon,"Content-Type":"application/json"},body:JSON.stringify(body)});const primaryData=await primary.json().catch(()=>null);if(primary.ok&&primaryData&&!primaryData.error)return reply({...primaryData,clientAction:safeAction(primaryData.clientAction),provider:primaryData.provider||"gemini",model:primaryData.model||"gemini-2.5-flash-lite"});const key=Deno.env.get("GROQ_API_KEY");if(!key)return reply({error:primaryData?.error||"Serviço temporariamente indisponível."},503);const model=Deno.env.get("GROQ_CHAT_MODEL")||"llama-3.1-8b-instant";const fallback=await fetch("https://api.groq.com/openai/v1/chat/completions",{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({model,messages:[{role:"system",content:"Você é o Synapse da NeuroNex. Responda em português brasileiro, com precisão e concisão. Não invente dados, não afirme executar ferramentas e nunca exponha rotas, URLs, IDs ou detalhes técnicos."},{role:"user",content:String(body.message||"")}],temperature:.3,max_completion_tokens:900})});const fallbackData=await fallback.json().catch(()=>null);if(!fallback.ok)return reply({error:fallbackData?.error?.message||"Serviço temporariamente indisponível."},503);return reply({response:String(fallbackData?.choices?.[0]?.message?.content||"Não consegui concluir agora."),clientAction:null,session_id:body.sessionId||body.session_id,provider:"groq",model,fallback:true})}catch(error){return reply({error:error instanceof Error?error.message:"Falha no gateway."},500)}});